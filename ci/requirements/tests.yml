name: pygmt
channels:
    - conda-forge
    - nodefaults
dependencies:
    # Required dependencies
    - gmt
    - numpy>=1.21
    - pandas
    - xarray
    - netCDF4
    - packaging
    # Development dependencies (general)
    - build
    - dvc
    - make
    - pip
    # Dev dependencies (unit testing)
    - pytest-cov
    - pytest-doctestplus
    - pytest-mpl
    - pytest>=6.0
    - sphinx-gallery
